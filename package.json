{
  "name": "polyblast-arena",
  "version": "0.1.0",
  "private": true,
  "description": "Phaser arena shooter with a lightweight WebSocket multiplayer server.",
  "scripts": {
    "start": "node server.js",
    "serve": "python -m http.server 5173 --bind 0.0.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
